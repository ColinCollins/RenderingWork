// const Data = require('../lib/Data');

window.onload = main();

let canvasWidth = 0;
let canvasHeight = 0;

function main () {
    let canvas = $('#webgl');
    canvasWidth = canvas.width;
    canvas.Height = canvas.height;
}